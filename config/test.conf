server {
    listen 8081;
	server_name localhost;
    host 127.0.0.1;
    root pages;
    client_max_body_size 3000000;
	index index.html;
    error_page 404 errors/404.html;

    location /test {
        allow_methods get;
        root pages;
        index post-request-data.html;
        autoindex off;
    }

    # location /sign-in {
    #     allow_methods get post;
    #     root pages/;
    #     index index.html;
    #     autoindex on;
    # }

	# location /red {
    #     allow_methods post delete;
	# 	return /tours;
	# }

    # location /cgi-bin {
    #     root ./;
    #     allow_methods get post delete;
    #     index time.py;
    # }

    # cgi_path /bin/bash;
    # cgi_ext .sh;
}